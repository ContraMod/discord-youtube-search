/** Rename or copy this file from `config.sample.ts` to `config.ts` and move it into `src` */

/** token used for youtube data api */
export const googleToken = ''

/** token used for discord bot */
export const discordToken = ''

/** Your discord user ID (NOT THE BOT'S ID) */
export const ownerID = ''

/** The ID of the discord channel the bot will run in */
export const channelID = ''